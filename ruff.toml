target-version = "py312"
line-length = 100

# Autogenerated boilerplate, not hand-written app code.
extend-exclude = ["alembic/versions"]

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[lint.isort]
known-first-party = ["app"]
